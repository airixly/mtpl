define([
    "jquery",
    "underscore"
], function ($, _) {
    var App = (function () {
        return {
            init: function () {
                return this;
            },
            start: function () {

            }
        }
    })();
    return App.init();
});
